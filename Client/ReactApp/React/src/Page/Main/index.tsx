import * as React from "react";
import * as stylesheet from "./CSS/stylesheet.scss";

const Main: React.FunctionComponent<any> = () => {
    return (
        <main className={stylesheet.main}>
            <h1>CESAR AWS ReactApp</h1>
        </main>
    );
};

export default Main